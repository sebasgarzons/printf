#include <limits.h>
#include <stdio.h>
#include "holberton.h"
/**
 * main - Entry point
 *
 * Return: Always 0
 */
int main(void)
{
	_printf("%d %d\n", INT_MAX, INT_MIN);
	printf("%d %d", INT_MAX, INT_MIN);
	return (0);
}
